#!/usr/bin/env python3
import sys, pathlib
sys.path.append( f"{pathlib.Path(__file__).parent}/tests" )
del sys, pathlib

import fire

from answer.ui import answer
from server import serve
from labels import label
from files import clear
from files import list

if __name__ == "__main__":
    fire.Fire()

#!/usr/bin/env bash

